services:
  - type: web
    name: phishing-detection
    env: python
    buildCommand: |
      pip install -r requirements.txt
      mkdir -p logs
    startCommand: gunicorn wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
      - key: SECRET_KEY
        generateValue: true